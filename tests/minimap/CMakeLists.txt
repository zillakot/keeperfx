cmake_minimum_required(VERSION 3.20)
project(minimap_fixture C)
set(CMAKE_C_STANDARD 11)
get_filename_component(KFX_ROOT "../.." ABSOLUTE)
file(READ "${KFX_ROOT}/src/frontmenu_ingame_map.c" SOURCE)
string(FIND "${SOURCE}" "void panel_map_draw_pixel(" START)
string(FIND "${SOURCE}" "/**\n * Draws single call to arms overlay" END)
math(EXPR LENGTH "${END} - ${START}")
string(SUBSTRING "${SOURCE}" ${START} ${LENGTH} ADAPTER)
string(FIND "${SOURCE}" "static void map_slabs_native(int32_t shift_x, int32_t shift_y, int32_t shift_stl_x, int32_t shift_stl_y)\n{" START)
string(SUBSTRING "${SOURCE}" ${START} -1 SLABS)
string(FIND "${SOURCE}" "void setup_background(long units_per_px)" START)
string(FIND "${SOURCE}" "void setup_panel_colors(void)" END)
math(EXPR LENGTH "${END} - ${START}")
string(SUBSTRING "${SOURCE}" ${START} ${LENGTH} BACKGROUND)
file(READ "${KFX_ROOT}/src/frontmenu_ingame_tabs.c" TABS)
string(FIND "${TABS}" "struct Around const draw_square[] = {" START)
string(FIND "${TABS}" "const short pixels_needed[]" END)
math(EXPR LENGTH "${END} - ${START}")
string(SUBSTRING "${TABS}" ${START} ${LENGTH} PATTERN)
string(FIND "${SOURCE}" "enum PanelColourIds" START)
string(FIND "${SOURCE}" "/**************************/" END)
math(EXPR LENGTH "${END} - ${START}")
string(SUBSTRING "${SOURCE}" ${START} ${LENGTH} ENUMS)
file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/minimap_enums.inc" "${ENUMS}")
string(FIND "${SOURCE}" "void setup_panel_colors(void)" START)
string(FIND "${SOURCE}" "void auto_gen_tables(long units_per_px)" END)
math(EXPR LENGTH "${END} - ${START}")
string(SUBSTRING "${SOURCE}" ${START} ${LENGTH} COLOURS)
file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/minimap.inc" "${PATTERN}\n${ADAPTER}\n${BACKGROUND}\n${COLOURS}\n${SLABS}")
add_executable(minimap_fixture fixture.c)
target_include_directories(minimap_fixture PRIVATE "${KFX_ROOT}/src" "${CMAKE_CURRENT_BINARY_DIR}")
target_compile_options(minimap_fixture PRIVATE -fwrapv -fsanitize=address,undefined -fno-omit-frame-pointer)
target_link_options(minimap_fixture PRIVATE -fsanitize=address,undefined)
if(UNIX AND NOT APPLE)
    target_link_libraries(minimap_fixture PRIVATE m)
endif()
enable_testing()
add_test(NAME minimap_fixture COMMAND minimap_fixture "${CMAKE_CURRENT_BINARY_DIR}/minimap.bin")
